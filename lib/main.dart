import 'package:flutter/material.dart';

import 'app/app.dart';

void main() {
  runApp(const App());
}

class Assgnment extends StatelessWidget {
  const Assgnment({super.key});

  @override
  Widget build(BuildContext context) {
    return const App();
  }
}
